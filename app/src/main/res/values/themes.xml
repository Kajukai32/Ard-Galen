<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JJOO2024SegundaParte" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="android:buttonStyle">@style/ButtonColor</item>
        <item name="colorButtonNormal">@color/login_buttons</item>
    </style>

    <style name="Theme.JJOO2024SegundaParte" parent="Base.Theme.JJOO2024SegundaParte" />
    <!-- res/values/styles.xml -->
    <!--    <item name="colorButtonNormal">@color/login_buttons</item>-->
    <!--    <item name="colorControlHighlight">@color/login_et_background</item>-->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="android:buttonStyle">@style/ButtonColor</item>
        <item name="colorButtonNormal">@color/white</item>
    </style>


    <style name="ButtonColor" parent="@android:style/Widget.Button">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>